# Aurea — Mobile App

Aplicación móvil de Aurea desarrollada con React Native (Expo) para iOS y Android.

## Scripts

```bash
npm install
npm run start     # Metro bundler
npm run android   # Ejecutar en emulador Android
npm run ios       # Ejecutar en simulador iOS (solo macOS)
```
